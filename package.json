{
  "name": "@acme/platform",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=22",
    "pnpm": "10.15.1"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "affected": "turbo run build test --affected",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/node": "24.3.1",
    "esbuild": "0.25.9",
    "turbo": "2.5.6",
    "typescript": "5.9.2"
  }
}
